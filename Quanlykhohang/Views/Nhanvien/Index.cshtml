@using PagedList.Mvc;
@using PagedList;
@model IPagedList<Quanlykhohang.Models.nhanvien>
@{
    ViewBag.Title = "Index";
}

<div class="card mt-5">
    <div class="card-header bg-danger text-white">
        <h3 class="font-weight-bold text-center">DANH SÁCH NHÂN VIÊN</h3>
    </div>

    <div class="card-body">
        <div class="row">
            <!--thuộc tính form-row nó giúp chia 2 cột trên 1 row của nó-->
            <div class="col-3 ">
                @Html.ActionLink("Thêm Mới Nhân Viên", "Create", null, new { @class = " btn btn-primary mt-3" })
            </div>

        </div>
        <br />
        <table class="table table-bordered">

            <tr>
                <th style="text-align:center">Mã Nhân Viên</th>
                <th style="text-align:center">Tên Nhân Viên</th>
                <th style="text-align:center">Giới Tính</th>
                <th style="text-align:center">Số Điện Thoại</th>
                <th style="text-align:center">Địa Chỉ</th>
                <th style="text-align:center">Tuỳ Chỉnh</th>

            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.ma_nv
                    </td>
                    <td>
                        @item.hoten_nv
                    </td>
                    <td>
                        @item.gioitinh_nv
                    </td>
                    <td>
                        @item.std_nv
                    </td>
                    <td>
                        @item.diachi_nv
                    </td>
                    <td>
                        @Html.ActionLink("Sửa", "Edit", new { id = item.id }, new { @class = "btn bg-warning" }) |
                        @Html.ActionLink("Chi Tiết", "Details", new { id = item.id }, new { @class = "btn bg-danger" }) |
                        @Html.ActionLink("Xóa", "Delete", new { id = item.id }, new { @class = "btn bg-info" })
                    </td>
                </tr>
            }
        </table>
        <br />
        Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)/@Model.PageCount
        <div>
            @Html.PagedListPager(Model, page => Url.Action("Index",
        new { page = page, @pageSize = ViewBag.pageSize }))
        </div>
    </div>
</div>
